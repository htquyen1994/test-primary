# Crypto Trading System — Backend Dependencies
# Pin exact minor versions; patch versions may be updated freely.

# Exchange connectivity
ccxt==4.3.89

# Async Redis (WebSocket tick writer)
redis==5.0.8
aioredis==2.0.1

# Task queue
celery==5.3.6
kombu==5.3.4

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
websockets==12.0

# Database (SQLAlchemy — SQL Server primary, SQLite fallback for tests)
sqlalchemy==2.0.31
pyodbc==5.3.0                  # SQL Server ODBC driver
# Uncomment for PostgreSQL:
# psycopg2-binary==2.9.9

# Config & validation
pydantic==2.7.4
pydantic-settings==2.3.4
pyyaml==6.0.1

# Data processing
pandas==2.2.2
numpy==1.26.4

# Technical indicators
# ATR, RSI, ADX, EMA, Bollinger Bands implemented from scratch in indicators/
# No external TA library required — avoids Python version compatibility issues

# File watching (config hot-reload)
watchdog==4.0.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
hypothesis==6.100.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.0          # async HTTP client for tests
